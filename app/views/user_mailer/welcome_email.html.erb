<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>It's appraisal time!, <%= @receiver.first_name %> <%= @receiver.last_name %></h1>
    <p>
      Please fill out the following surveys 
    <br>
    </p>
    <p>

    <% if @receiver.job_title == 'Sales Person' %>
      <%= @url_salesperson_peer %>
    <% end %>

    <% if @receiver.job_title == 'Sales Intern' %>
      <%= @url_salesperson_subordinate %>
    <% end %>

    <% if @receiver.job_title == 'Sales Manager' %>
      <%= @url_salesperson_supervisor %>
    <% end %>

    </p>
    <p>Goooooooo Groupon! (olivia)</p>
  </body>
</html>