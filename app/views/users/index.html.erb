
<h1>Groupon SA Roster</h1>


<% @users.each do |user| %>
	<% name = user.first_name + " " + user.last_name %>
	<%= link_to name, user_path(user) %> - <%= user.job_title %>
	<br/>

<% end %>

<% if current_user.role == 'admin' %>
	<%= button_to "Add New Employee", new_user_registration_path, method: :get %><br>
<% end %>

<%= button_to "Add New Employee", new_user_path, :method => :get %>